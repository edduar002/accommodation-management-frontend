/**
 * Archivo de configuración global de la aplicación.
 * Contiene variables y constantes que se usan en todo el proyecto.
 */
export var global = {
  // URL base para las peticiones HTTP al backend
  url: 'http://localhost:8081/api/',
};